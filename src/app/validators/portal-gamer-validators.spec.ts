import { PortalGamerValidators } from './portal-gamer-validators';

describe('PortalGamerValidators', () => {
  it('should create an instance', () => {
    expect(new PortalGamerValidators()).toBeTruthy();
  });
});

